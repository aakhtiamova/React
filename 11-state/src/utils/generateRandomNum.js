function generateRandomNum(maxNum) {
  return Math.floor(Math.random() * maxNum); // default method random for return random number from 0 to 100. math.floor used for make integers numbers
}

export default generateRandomNum;
